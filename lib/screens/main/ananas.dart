import 'package:flutter/material.dart';

class Ananas extends StatelessWidget {
  const Ananas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      body: Center(child: Text("data"),),);
  }
}